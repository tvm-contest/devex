extends layout

block content
  
  .forms
    .container
        form(method='post' enctype='multipart/form-data').deploy-from-file
            input.text(name='jsonCollection', type="file").btn-blue
            br
            button.text.btn-submit(type='submit' onClick='loader(this)').btn.btn-blue="Deploy"
              span(class="" role="status" aria-hidden="true")