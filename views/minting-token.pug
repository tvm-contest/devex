extends layout

block content
  .forms
    .container
      form.row.g-3#form-contract(action="/minting-tokens/minting", method="post") 
        input(name="rootAddress" value=collectionRootAddress type="hidden")
        input(name="contractName" value=collectionName type="hidden")
        .content
          .rarities
        
          .parameters 
          
          .mediafile 
          
            
        button.submit-button.btn.btn-blue.btn-block(type="submit" value="Minting token" onClick="loader(this)")= "Minting token "
          span(class="" role="status" aria-hidden="true")
    
  script. 
    var address = `!{FileJson}`
    //console.log(address)
  script(type="module" src="/scripts/minting-token.js")
  
        