; generated by drom from package skeleton 'driver'
(executable
  (name main)
  (public_name sudoku_generator)
  (package sudoku_generator)
  (modules main_cpp main_hpp main)
  (libraries ez_file )
  
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package sudoku_generator))


