# Auto-generated by ft contract --new

all: contracts checks


SIBLING_DIR=../../03_pincode/contracts

checks:
	diff Blueprint.sol ${SIBLING_DIR}/Blueprint.sol
	diff RecoverablePubkey.sol ${SIBLING_DIR}/RecoverablePubkey.sol

INTERFACES=I*.sol

clean:
	rm -f *~ *.code

distclean: clean
	rm -f *.tvm *.abi.json


contracts::EulerRoot.code

EulerRoot.code: *.sol
	ft contract build EulerRoot.sol -f


contracts::EulerProblem.code

EulerProblem.code: *.sol
	ft contract build EulerProblem.sol -f


contracts::EulerUser.code

EulerUser.code: *.sol 
	ft contract build EulerUser.sol -f
